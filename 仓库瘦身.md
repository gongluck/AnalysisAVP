# 仓库瘦身

## 命令

```shell
# 切换目标分支（以master为目标分支为例）
git checkout master
# 拉取仓库mains分支的最新版本
git pull
# 本地备份
# 删除分支
git branch -D master
# 在master分支下创建分支
git chekcout master
# 覆盖代码
# 强制推送到远程
git push -f -u origin master
```

